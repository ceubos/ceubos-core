#!/bin/sh
# Creates an "Install EducatuX" icon on the live session tested with:
# Xfce, LXDE, LXqt, Gnome, KDE, Mate, Cinnamon


# Create ~/Desktop just in case this runs before the xdg folder
# creation script.
mkdir -p ~/Desktop


# Among the Debian desktop environments, LXDE is the only one
# that behaves completely different.
if [ -f /usr/bin/lxsession ]; then
    echo "[Desktop Entry]" > ~/Desktop/install-educatux.desktop
    echo "Type=Link" >> ~/Desktop/install-educatux.desktop
    echo "Name=Install EducatuX" >> ~/Desktop/install-educatux.desktop
    echo "Icon=install-educatux" >> ~/Desktop/install-educatux.desktop
    echo "URL=/usr/share/applications/install-educatux.desktop" \
          >> ~/Desktop/install-educatux.desktop
else
    cp /usr/share/applications/install-educatux.desktop ~/Desktop
    # Xfce needs this executable otherwise it complains, everything
    # else doesn't seem to care either way.
    chmod +x ~/Desktop/install-educatux.desktop
fi
